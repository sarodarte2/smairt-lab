"""SMAIRT: a small CLI that scaffolds and audits disciplined research projects.

This file is the top of the package. It doesn't do any work itself — it just
exposes the package version (``__version__``) so other modules (and
``smairt.yaml`` files they write) can record which SMAIRT generated them.

If you're new to this codebase, start at ``src/smairt/cli.py`` (the commands
a researcher types) or read ``docs/ARCHITECTURE.md`` for the full tour.
"""

__version__ = "0.4.0"
